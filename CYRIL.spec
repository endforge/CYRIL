# -*- mode: python ; coding: utf-8 -*-
from PyInstaller.utils.hooks import collect_submodules
from PyInstaller.utils.hooks import collect_all

datas = [('C:\\CYRIL\\scripts\\ui\\pywebview\\index.html', 'scripts/ui/pywebview'), ('C:\\CYRIL\\scripts\\ui\\pywebview\\CYRIL.ico', 'scripts/ui/pywebview')]
binaries = []
hiddenimports = []
hiddenimports += collect_submodules('scripts')
hiddenimports += collect_submodules('common')
tmp_ret = collect_all('keyring')
datas += tmp_ret[0]; binaries += tmp_ret[1]; hiddenimports += tmp_ret[2]


a = Analysis(
    ['C:\\CYRIL\\scripts\\ui\\pywebview\\app.py'],
    pathex=['C:\\CYRIL'],
    binaries=binaries,
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='CYRIL',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['C:\\CYRIL\\scripts\\ui\\pywebview\\CYRIL.ico'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='CYRIL',
)
